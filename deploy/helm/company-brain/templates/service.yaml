apiVersion: v1
kind: Service
metadata:
  name: company-brain-app
spec:
  selector:
    app.kubernetes.io/name: company-brain
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 80
      targetPort: http
