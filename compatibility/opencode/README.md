# OpenCode Compatibility Pack

OpenCode-compatible exports should include:

- `.opencode/skills/<name>/SKILL.md`
- `.opencode/tools/<tool>.ts`
- `opencode.json` permissions that hide blocked skills and require confirmation for risky tools

This repo exposes the canonical generator in `lib/adapters.ts`.
