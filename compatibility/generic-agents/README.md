# Generic Agent Compatibility Pack

This is the lowest common denominator export for agents that support `AGENTS.md`, `.agents/skills`, or plain MCP configuration.

The canonical registry should generate these files per tenant and per tier, filtered by ACL and package status.
