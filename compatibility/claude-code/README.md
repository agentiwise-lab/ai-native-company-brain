# Claude Code Compatibility Pack

The runtime generator emits `.claude/skills/<name>/SKILL.md` files plus MCP configuration for the Company Brain endpoint.

The system should distribute only reviewed and published skills. Draft or blocked registry items should not appear in generated packs for ordinary users.
